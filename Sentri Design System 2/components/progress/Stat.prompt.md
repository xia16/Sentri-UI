Glanceable workload number (44px condensed) with an uppercase micro-label — the summary row on Today and unit screens.

```jsx
<Stat value={31} label="Due now" />
<Stat value={6} label="Overdue" tone="overdue" />
<Stat value={18} suffix="of 31" label="Recorded" />
```
