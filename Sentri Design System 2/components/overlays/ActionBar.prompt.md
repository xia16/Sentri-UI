Sticky raised bar above the bottom nav during bulk selection — selection count on the left, actions on the right.

```jsx
<ActionBar message="8 selected"><Button variant="secondary">Clear</Button><Button icon="check">Record</Button></ActionBar>
```
