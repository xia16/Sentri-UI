48px text field with optional label, leading icon, unit suffix, hint/error.

```jsx
<Input label="Ear tag" icon="search" placeholder="Scan or type ID" value={q} onChange={setQ} />
<Input label="Feed amount" suffix="kg" type="number" error="Above curve maximum" />
```
