Square icon-only button (48px, 36px sm) for scan, search, filter, back — always pass `label`.

```jsx
<IconButton icon="scan-line" label="Scan ear tag" variant="primary" />
<IconButton icon="sliders-horizontal" label="Filter" />
```
