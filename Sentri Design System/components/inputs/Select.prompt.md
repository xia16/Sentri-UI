Native single-pick dropdown styled to the 48px control system — used for result values, reasons, destinations.

```jsx
<Select label="Result" placeholder="Choose result" options={['Pregnant','Not pregnant','Recheck']} value={r} onChange={setR} />
```
