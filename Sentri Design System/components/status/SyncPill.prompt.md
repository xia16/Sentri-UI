Connectivity/sync state indicator — barns have unreliable network, so recorded work must show whether it reached the server.

```jsx
<SyncPill state="synced" />
<SyncPill state="pending" count={2} />
<SyncPill state="offline" />
```
